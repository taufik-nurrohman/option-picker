extends ../_index.pug

block state
  - const scripts = ['../index.min.js']
  - const styles = ['../index.min.css']
  - const title = 'Tag Picker'

block script
  script
    | document.querySelectorAll('input[name="test[]"]').forEach(self => {
    |     const picker = new OptionPicker(self);
    |     picker.self.form.addEventListener('submit', function (e) {
    |         console.log(this.value);
    |         this.prevent.checked && e.preventDefault();
    |     });
    | });

block style
  style
    | .option-picker {
    |   min-width: 10em;
    | }
    | .option-picker__options {
    |   background: #fff;
    | }

block content
  main
    p Submitting a form by pressing the #[em Submit] button vs. pressing the #[kbd Enter] key in the option picker mask.
    form(method='get' target='_blank')
      p: input(list='options' name='test[]' type='text')
      p: input(list='options' name='test[]' type='text' value='Option 2')
      p: input(list='options' name='test[]' type='text' value='3')
      p
        label
          input(name='prevent' type='checkbox')
          = ' '
          span Test Default Prevention via JavaScript
      p
        button(type='submit') Submit
        = ' '
        button(type='reset') Reset
      datalist#options
        option Option 1
        option Option 2
        option(value='3') Option 3