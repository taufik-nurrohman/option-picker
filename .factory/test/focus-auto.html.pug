extends ../_index.pug

block state
  - const scripts = ['../index.min.js']
  - const styles = ['../index.min.css']
  - const title = 'Option Picker'

block script
  script
    | const picker = new OptionPicker(document.querySelector('select'));

block style
  style
    | .option-picker__options {
    |   background: #fff;
    | }

block content
  main
    p
      select(autofocus id='test')
        option Option 1
        option Option 2
        option(value='3') Option 3
    p #[label(for='test' style={'text-decoration': 'underline'}) Click here] to focus to the option picker!